import {Meta,Canvas,Story,ArgsTable} from '@storybook/addon-docs/blocks';
import {{pascalCase (getPasta name)}} from '../{{pascalCase (getPasta name)}}';

<Meta title="Components/Buttons" component={ {{pascalCase (getPasta name)}} }/>

# 🔘→{{pascalCase (getPasta name)}}
_________

__Description:__

<!-- digite aqui a descrição -->
_________

<Canvas>
  <Story
    name="{{pascalCase (getPasta name)}}"
    args=""
  >
    { (args) => < {{pascalCase (getPasta name)}}  {...args} /> }
  </Story>
</Canvas>

<ArgsTable of={ {{pascalCase (getPasta name)}} } />
