import React, { useContext, useEffect,  useState  } from "react"

import { Container } from './styles';
{{!-- import { observer} from 'mobx-react-lite'; --}}
{{!-- import {{pascalCase (getName name)}}Store  from "../../stores/{{pascalCase (getPasta name)}}Store" --}}

/**
*@Author
*@Issue
*/

function {{pascalCase (getPasta name)}}(){
  {{!-- const store = useContext({{pascalCase (getPasta name)}}Store); --}}

  return <Container>
    
         </Container>;
}
export default {{pascalCase (getPasta name)}};