import React from 'react';
import { Container } from './styles';

interface {{pascalCase (getName name)}}Props{
  //adicionar os props
}

export const {{pascalCase (getName name)}}: React.FC< {{pascalCase (getName name)}}Props> = () => {
  return <Container>
    
         </Container>;
}